Small script to pull all FLARM IDs from OGN Device DB, filter aircraft of interest, and output a file in XCSoar compatible format.

This is useful as XCSoar supports a max of 200 FLARM Identifications.